import * as AEO from './aeo';

export { AEO };

// Also export types at top level for convenience
export type {
  Article,
  ArticleSummary,
  ArticlesResponse,
  Category,
  Tag,
  ClientOptions,
} from './aeo';
